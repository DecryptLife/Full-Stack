git-init => initializes the current folder as a git repository

git-status => current status of the folder

git add . => all the files in the current directory will be added to staging area

git commit => commit the changes to the git repository

git log --oneline => log of all commits put into repository

git checkout <commit><file> => checkout the file from an older commit ie if we want to change the file to an older commit